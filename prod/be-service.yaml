apiVersion: v1
kind: Service
metadata:
  name: eventapp-be-svc # switcher-service
  namespace: eventapp-prod
spec:
  type: ClusterIP
  selector:
    app: eventapp-be
    version: ${COLOR} # <-- Points dynamically to BLUE or GREEN deployment
  ports:
    - port: 80
      targetPort: 8080
